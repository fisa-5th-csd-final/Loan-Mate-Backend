spring:
  application:
    name: loan-mate
  config:
    import:
      - classpath:application-db.yml
      - classpath:application-auth.yml
  profiles:
    active: ${PROFILE}

server:
  port: ${PORT:8080}
  servlet:
    session:
      cookie:
        name: LOANMATESESSION
